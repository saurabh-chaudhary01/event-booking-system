server:
  port: 8010

spring:
  application:
    name: user-service
  datasource:
    url: jdbc:postgresql://localhost:6020/user_db
    username: saurabh
    password: saurabh123
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect

eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://localhost:8761/eureka